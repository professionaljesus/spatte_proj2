% Load data
clear
close all
load fmri.mat


col_img = colstack(img);
G = 4*speye(8874,8874);

up = -1;
down = 1;
left = 0;
right = 0;
for i = 1:8874
    up = i - 1;
    if mod(i,88) == 0 || i == 1
        up = i;
    end
    down = i + 1;
    if mod(i, 87) == 0
        down = i;
    end
    
    
    
    
    left = i - 87;
    if i <
    right = i + 87;
end

%Gibb loop



%Posterior expectation/variance
% mean(beta) ?? std(beta)?????


%Significant pixel
